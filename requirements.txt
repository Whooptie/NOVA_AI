# ============================================================
# Nova AI — Python dependencies
# ============================================================
# Installeren via: pip install -r requirements.txt
# Python 3.10+ vereist
# ============================================================

# --- Schaak ---
python-chess>=1.999        # Stockfish interface + bordbeheer

# --- Weer ---
requests>=2.31.0           # OpenWeatherMap API calls

# --- Wikipedia ---
wikipedia-api>=0.6.0       # Wikipedia AutoTeacher

# --- Omgevingsvariabelen ---
python-dotenv>=1.0.1       # .env bestand lezen (API keys)

# --- JSON validatie ---
jsonschema>=4.21.0         # identity.json / schema.json validatie

# --- Layer 5: Context & Activity Awareness ---
pygetwindow>=0.0.9         # activity_detector.py — actief venster/proces detecteren
mediapipe>=0.10.9          # presence_detector.py — BlazeFace gezichtsdetectie (bounded ML-tool, geen LLM)

# --- Layer 6: Personality / Adaptive Learning ---
scikit-learn>=1.4.0        # train_classifier.py / microlearning.py — signal_model.pkl (bounded ML-classifier, geen LLM)

# --- Testing ---
pytest>=8.2.0              # Unit tests

# --- Standaardbibliotheek (geen installatie nodig) ---
# sqlite3      — Layer 0 geheugen (ingebouwd in Python)
# threading    — achtergrondtimers, daemon threads
# json         — concepts.json, alle databestanden
# re           — reguliere expressies in intent_router
# datetime     — tijdsbewustzijn, dagpatronen
# logging      — RotatingFileHandler
# subprocess   — Stockfish proces, reboot
# atexit       — graceful shutdown hooks
# pathlib      — portabele bestandspaden
# ctypes       — focus_detector.py (Windows GetLastInputInfo)
# pickle       — signal_model.pkl in-/uitladen