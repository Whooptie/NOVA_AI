# ============================================================
# Nova AI — Python dependencies
# ============================================================
# Install via: pip install -r requirements.txt
# Python 3.10+ required
# ============================================================

# --- Chess ---
python-chess>=1.999        # Stockfish interface + board management

# --- Weather ---
requests>=2.31.0           # OpenWeatherMap API calls

# --- Wikipedia ---
wikipedia-api>=0.6.0       # Wikipedia AutoTeacher

# --- Environment variables ---
python-dotenv>=1.0.1       # Reads the .env file (API keys)

# --- JSON validation ---
jsonschema>=4.21.0         # identity.json / schema.json validation

# --- Layer 5: Context & Activity Awareness ---
pygetwindow>=0.0.9         # activity_detector.py — active window/process detection
mediapipe>=0.10.9          # presence_detector.py — BlazeFace face detection (bounded ML tool, not an LLM)

# --- Layer 6: Personality / Adaptive Learning ---
scikit-learn>=1.4.0        # train_classifier.py / microlearning.py — signal_model.pkl (bounded ML classifier, not an LLM)

# --- Testing ---
pytest>=8.2.0              # Unit tests

# --- Standard library (no installation needed) ---
# sqlite3      — Layer 0 memory (built into Python)
# threading    — background timers, daemon threads
# json         — concepts.json, all data files
# re           — regular expressions in intent_router
# datetime     — time awareness, day patterns
# logging      — RotatingFileHandler
# subprocess   — Stockfish process, reboot
# atexit       — graceful shutdown hooks
# pathlib      — portable file paths
# ctypes       — focus_detector.py (Windows GetLastInputInfo)
# pickle       — loading/saving signal_model.pkl