# 🗂️ Nova — Changelog

> Archief van opgeloste bugs en afgeronde werkpunten, verplaatst vanuit
> `nova_state.md` op 18 juli 2026 om dat bestand overzichtelijk te
> houden. `nova_state.md` blijft de plek voor: architectuur, huidige
> laag-status, en NOG OPEN bugs/werkpunten. Dit bestand is puur
> geschiedenis — niet nodig om bij elk nieuw gesprek te uploaden,
> enkel handig als je wil terugzoeken wanneer/hoe iets werd opgelost.
>
> Volgorde: chronologisch, oudste eerst, zoals ze oorspronkelijk in
> `nova_state.md` stonden.

---

## ✅ Opgeloste bugs

| # | Bug | Bestand | Urgentie | Oplossing |
| --- | --- | --- | --- | --- |
| 1 | OpenWeatherMap API-key hardcoded én gelekt in chat. | weather.py | 🔴 DIRECT | Opgelost (2 juli 2026 — key naar .env, python-dotenv) |
| 1b | Stad-extractie pakte leestekens mee (bv. "gent?" i.p.v. "gent") | weather.py | 🟢 Laag | Opgelost (2 juli 2026 — strip(".,!?;:") toegevoegd) |
| 1c | Weer-intent werd niet herkend bij korte zinnen ("weer in Gent?") | intent_router.py | 🟡 Medium | Opgelost (2 juli 2026 — losse woord-detectie i.p.v. vaste triggerzinnen) |
| 1d | Weekdagnaam kwam in het Engels terug ("Monday" i.p.v. "maandag") | weather.py | 🟢 Laag | Opgelost (3 juli 2026 — eigen NL-weekdaglijst i.p.v. strftime %A) |
| 2 | Windows-pad hardcoded in save_path | memory.py | 🟡 Medium | Opgelost (2 juli 2026 — portable pad via Path(**file**), werkt op elke PC/gebruiker) |
| 3 | Dode code (uitgecommentarieerde handlers) | chat.py | 🟢 Laag | Opgelost (11 juli 2026 — meegenomen bij bug #5: dode `on_weather_response()` + subscribe verwijderd uit `chat.py`, dode `time_response`/`weather_response`/`date_response`/`math_response`-afdruktakken verwijderd uit `main.py`) |
| 4 | overstimulation.level = 1.0 als standaard | emotion_state.json | 🟢 Laag | Opgelost (zie bug #11 voor de oorspronkelijke fix, en Layer 6-sectie 17 juli 2026 voor de latere karakterherziening die de trigger-stappen zelf ook verlaagde) |
| 5 | Personality/tone pipeline bypassed door weer/tijd/math | response_pipeline.py | 🟡 Medium | Opgelost (11 juli 2026 — `weather.py`, `time.py` en `math.py` publiceren nu `layer4_response` i.p.v. rechtstreeks `chat_response`/`math_response`; lopen zo alle drie door dezelfde tone-keten als definities. Bijvangst: `math.py` publiceerde voorheen `math_response`/`msg`, een event dat nergens meer werd afgeluisterd — rekenvragen waren dus stil; nu ook zichtbaar. Dode restanten opgeruimd: `chat.py`'s `on_weather_response()` + subscribe (zie ook bug #3), en de dode `time_response`/`weather_response`/`date_response`/`math_response`-afdruktakken in `main.py`.) |
| 11 | Nova's `emotion_state.json` stond structureel op een hoge `overstimulation.level` — elk antwoord, ongeacht onderwerp, kreeg het "overprikkeld_chaotisch_snel"-sjabloon (😵⚡💥) van `tone_engine.py`. Twee samenhangende oorzaken gevonden: (1) startwaarde stond op 1.0 (max), ver boven de threshold van 0.75 — Nova begon dus al overprikkeld bij het opstarten; (2) `emotion_engine.py` had geen enkel pad naar beneden — `apply_trigger()` verhoogde `overstimulation.level` bij triggers met `overflow_behavior` (bv. `excitement`, `focus`) met +0.15, maar niets liet het ooit weer zakken. | emotion_engine.py / Layer 6 | 🟡 Medium | Opgelost (11 juli 2026 — startwaarde naar 0.1 in `emotion_state.json`; `_apply_overstimulation_decay()` toegevoegd aan `emotion_engine.py` met TWEE decay-vormen die samenwerken: (a) tijd-gebaseerd — `last_trigger_timestamp` bijgehouden, elke minuut stilte laat het niveau zakken met `decay_per_minute` (standaard 0.05, instelbaar in `emotion_state.json` zonder code te wijzigen); (b) interactie-gebaseerd — triggers ZONDER `overflow_behavior` (bv. `confusion`) geven nu ook -0.05. Live getest: Nova start rustig bij `enthousiast_snel`, en bij snel herhaalde `excitement`-triggers (sneller dan de decay kan bijbenen) schakelt ze terecht over naar `overprikkeld_chaotisch_snel` — het systeem is nu omkeerbaar i.p.v. permanent vast te zitten.) |
| 12 | `apply_energy_modulation()`'s gewichten (0.7+0.3+0.2=1.2) telden op tot boven 1.0 — `current_energy` clampte bij hoge traits altijd naar de bovengrens en bleef daar structureel vastzitten (zelfversterkende val, de geclampte 1.0 voedde zichzelf terug in de volgende ronde). Ontdekt direct nadat `BehaviorModifiers` voor het eerst echt werd aangeroepen (zie Layer 6-sectie). | behavior_modifiers.py | 🟡 Medium | Opgelost (17 juli 2026 — gewichten herverdeeld naar 0.5/0.2/0.3, samen exact 1.0) |
| 13 | `update_state()`'s `energy_boost`/`energy_drop` clampten nooit tussentijds — een tussenwaarde kon nog steeds >1.0 zijn (bv. 0.9 + 0.40 = 1.30) vóór `apply_energy_modulation()` (bug #12's fix) daarmee ging rekenen, waardoor energie bij herhaalde triggers alsnog naar de bovengrens bleef geduwd worden ondanks correcte gewichten. | personality_engine.py | 🟡 Medium | Opgelost (17 juli 2026 — tussentijdse clamp direct na energy_boost/energy_drop, vóór de modifiers aan de beurt komen) |
| 14 | `gesture_profiles.json` werd door `tone_engine.py` wel ingeladen (`self.gesture_profiles`) maar nooit gekoppeld aan `tone["gesture_data"]` — `expression_injector.py`'s `_inject_gestures()` vond dus altijd lege data en deed in de praktijk nooit iets, ondanks dat het bestand al langer bestond. Vierde voorbeeld van hetzelfde "dode data"-patroon als personality_style/response_style/identity_state eerder deze sessie. | tone_engine.py | 🟢 Laag | Opgelost (17 juli 2026 — nieuwe `_apply_gesture_data()`-methode, plus nieuw `text_hint`-veld per profiel in `gesture_profiles.json`) |
| 15 | `antwoord_wie_ben_je()` las het statische `"age"`-veld uit identity.json, dat sinds de karakterherziening bewust op `null` staat — gaf dus altijd "onbekend jaar" terug in plaats van een zinvolle leeftijd. | self_query.py | 🟢 Laag | Opgelost (17 juli 2026 — gebruikt nu dezelfde `_bereken_leeftijd_tekst()`-helper als `antwoord_leeftijd()`) |
| 16 | `train_classifier.py`'s vergelijking `nieuwe_score >= huidige_score` crashte met een `TypeError` bij de allereerste training ooit, omdat er nog geen bestaand model was om `_evalueer_huidig_model()` tegen te draaien (gaf `None` terug). Gevonden en gefixt door Claude tijdens lokaal testen, VOOR Kevin het script ooit draaide. | train_classifier.py | 🟢 Laag | Opgelost (17 juli 2026 — expliciete `if huidige_score is None: wordt_actief = True`-tak toegevoegd) |
| 17 | `signal_trait_mapping.json` bevatte van meet af aan een `"focus"`-signaal, maar dat kwam nergens voor in `training_data.json` of de woordenlijst-fallback in `microlearning.py` — kon dus nooit herkend worden, ondanks dat de koppeling naar traits al wel bestond. | microlearning.py / training_data.json | 🟢 Laag | Opgelost (18 juli 2026 — 7 nieuwe trainingsvoorbeelden + 2 benchmark-voorbeelden + woordenlijst-uitbreiding toegevoegd, gevolgd door hertraining. Live bevestigd: focus_hyperfocus_tendency/chaotic_variability's tellers bewogen correct.) |
| 18 | `pattern_matcher.py` had nog twee tijdelijke testwaarden staan uit de ontwikkelfase: `MIN_OBSERVATIES_VOOR_ANOMALIE` op 3 i.p.v. een realistische productiewaarde, en `save_to_disk()` tellergebaseerd (`total % 2 == 0`) i.p.v. tijdgebaseerd. Dat laatste hield ook een stil risico in: bij `exit`/`/reboot` kon de laatst binnengekomen observatie nooit weggeschreven zijn als `total` net oneven was op dat moment. | pattern_matcher.py | 🟢 Laag | Opgelost (18 juli 2026 — drempel naar 10, opslag omgebouwd naar een `threading.Timer`-achtergrondlus die elke 5 min opslaat bij wijzigingen (`_dirty`-vlag), plus nieuwe `shutdown()` die bij afsluiten altijd nog één keer definitief opslaat. Project-brede audit bevestigde dat geen andere module hetzelfde risico had.) |
| 19 | Punt/vraagteken/uitroepteken/komma aan het einde van een woord werd meegenomen bij een definitie-vraag (bv. "wat is een gitaar." → woord `"gitaar."` i.p.v. `"gitaar"`). `.strip()` in `chat.py`'s `on_definition()` verwijdert enkel witruimte, geen leestekens — dat vervuilde woord ging vervolgens naar `get_meaning()`, de is_a-relatiecheck, ÉN de Wikipedia-fallback (`wikipedia_teacher.py`), waar het zowel de zoekopdracht kon laten falen als lelijke tekst gaf in foutmeldingen/teach-suggesties. | chat.py / wikipedia_teacher.py | 🟢 Laag | Opgelost (18 juli 2026 — `word.strip(".,!?;:")` toegevoegd in `chat.py`'s `on_definition()`, vlak na de lidwoord-stripping en vóór het woord waar dan ook gebruikt wordt. Extra defensief vangnet toegevoegd in `wikipedia_teacher.py`'s `on_wiki()` zelf, voor het geval een andere module ooit rechtstreeks een `intent_wiki`-event stuurt zonder via `chat.py` te lopen. Live getest: "wat is een gitaar.", "wat is een computer?", "wat betekent hond!" geven nu allemaal een schoon antwoord zonder leesteken in het woord. **Bijkomende observatie tijdens het testen (niet dezelfde bug, apart gehouden):** "wat is fysica?" toonde dat Wikipedia dit keer wél correct gevonden werd, maar dat `_extract_definition()` er soms nog steeds geen bruikbare definitie uit kan halen — mogelijk verwant aan bug #8 hieronder, nog niet onderzocht. Zie bug #8 in `nova_state.md` voor deze losse observatie.) |
| 20 | Oude/nieuwe `concepts.json`-entries kregen bijna nooit een automatische `is_a`-relatie via `_auto_extract_is_a()` in `semantic.py`. Oorzaak: de regex herkende enkel definities die LETTERLIJK beginnen met "een X met/die/dat/...", terwijl bijna elke Wikipedia-definitie in de praktijk het patroon "[Het woord] is een X die/dat/..." volgt (het woord zelf staat eerst). Op de volledige bestaande dataset (201 bruikbare definities) matchte de oude regex slechts 4 keer (2%). | semantic.py | 🟢 Laag | Opgelost (18 juli 2026 — drie nieuwe regex-patronen toegevoegd voor "[woord] is/zijn een X ...", plus de samengestelde "waarmee/waarin/waaruit/waardoor/waarop/waarvoor"-vormen naast het bestaande losse "waar". Dekking op dezelfde dataset steeg van 2% (4 matches) naar 28% (56 matches), geen foute match in de steekproef. Bewust GEEN herstelscript gebouwd voor bestaande senses — enkel nieuw geleerde woorden (via `teach` of Wikipedia) profiteren automatisch. Live bevestigd met "zeilboot" → automatisch `is_a: vaartuig`, `source: auto_extract`. Bewust nog NIET aangepakt: definities met een bijvoeglijk naamwoord tussen "een" en het echte target-zelfstandig-naamwoord (bv. "een grote, niet-giftige slang") — vraagt een POS-check per woord, apart werkpuntje voor later.) |
| 21 | `module_loader.py` vroeg Layer 1 op via `self.loaded_modules.get("word_associations")` (stap 3B voor `response_engine.py`, EN stap 3E voor het nieuwe `emergence_engine.py`) — maar de dynamische modules-lus slaat elke module op onder de kale bestandsnaam, dus de echte key is `"word_associations_learner"`. Beide plekken kregen daardoor stilzwijgend `None` in plaats van de echte Layer 1-instantie, zonder ooit een foutmelding (defensieve `if word_assoc else {}`-code ving het stil op). **Gevonden tijdens het bouwen en live testen van Layer 7** (20 juli 2026) — `emergence_engine.debug_layers_status()` toonde `word_associations_aanwezig: False` ondanks een correct gevulde `layers`-dict. Bevestigt meteen dat dit ook Layer 4 al sinds 8 juli trof: de "personal touch" (Fase 2, `_sterkste_associatie()`) kon dus nooit tonen, ondanks dat de code er wel voor gebouwd was. | module_loader.py | 🟡 Medium | Opgelost (20 juli 2026 — key op beide plekken (stap 3B `response_layers`, stap 3E `emergence_layers`) gecorrigeerd naar `"word_associations_learner"`. Live bevestigd op twee manieren: `emergence debug` toont nu `word_associations_aanwezig: True`, en "wat is python" toont voor het eerst een echte personal-touch-zin ("Dat associeer je hier trouwens opvallend vaak met 'favoriet'!") in plaats van enkel de kale definitie.) |

**Let op — nog open, NIET verplaatst (staan nog in `nova_state.md`):** #8 (Wikipedia-voorbeeldzin-extractie, plus de fysica-observatie), #9 (detect_pos werkwoord/zelfstandig-naamwoord-dubbelzinnigheid — enkel omzeild, niet structureel opgelost), #10 (Layer 1 sense-onbewustheid, wacht op disambiguatie-laag).

---

## ✅ Afgeronde werkpunten (voorheen "Volgende stappen")

**`behavior_modifiers.py`-koppeling — documentatie-correctie (18 juli 2026, geen nieuwe code-wijziging).** Werkpunt 6 in de "Volgende stappen"-lijst beweerde dat `BehaviorModifiers` (`apply_energy_modulation()`, `apply_impulsivity()`, `apply_dramatic_flair()`) nog steeds ongebruikt was. Bij nazicht van de echte code (18 juli 2026) bleek dit niet te kloppen: alle 3 methodes worden wel degelijk aangeroepen in `personality_engine.py`'s `update_state()` (`self.modifiers.apply_energy_modulation()` / `apply_impulsivity()` / `apply_dramatic_flair()`), vermoedelijk stilzwijgend gekoppeld tijdens de karakterherziening-sessie van 17 juli 2026 (dezelfde sessie waarin de twee gewichten-bugs #12/#13 ontdekt en opgelost werden), maar nooit teruggemeld in de "Volgende stappen"-lijst zelf. Live bevestigd via `identity_state.json`: `impulsivity_modulation: 0.3` en `dramatic_flair_state: 0.15` komen exact overeen met `traits.json`'s `impulsivity`/`dramatic_flair`-waarden, wat enkel kan als `apply_impulsivity()`/`apply_dramatic_flair()` daadwerkelijk draaien. Werkpunt 6 is dus verwijderd uit de open "Volgende stappen"-lijst.

**Personality pipeline (deel 1)** — uitgebreid naar weer/tijd/math (11 juli 2026, zie bug #5 en `layer4_response`-sectie in `nova_state.md`). Nu door de tone-pipeline: greeting, fallback, Layer 4-definities, weer, tijd, math.

**Personality pipeline (deel 2)** — opgelost (12 juli 2026). Alle 10 semantic-gerelateerde handlers in `chat.py` publiceren nu `layer4_response` i.p.v. rechtstreeks `chat_response`: `on_relation_check`, `on_part_of_check`, `on_subtypes_query`, `on_related_to`, `on_synonym`, `on_antonym`, `on_used_for`, `on_causes`, `on_properties`, `on_meaning`. Zowel het succespad als het "geen resultaat"-pad (bv. "Welk woord bedoel je?") lopen nu door de tone-pipeline, voor een consistente warme toon ongeacht uitkomst. `on_greeting` bewust ongewijzigd gelaten — die loopt al via `response_pipeline.py`'s eigen, aparte `on_greeting()`-handler (zelfde patroon als `on_fallback()`), dus ombouwen naar `layer4_response` zou dat bestaande specifieke pad juist doorbreken. Schaken en help blijven bewust rechtstreeks op `chat_response` (zie `layer4_response`-sectie) — schaken wordt herbekeken zodra Kevin's huidige chess-uitbreidingen klaar zijn.

*Bijvangst tijdens het testen: `related_to` had geen enkele trigger in `intent_router.py`.* `on_related_to()` in `chat.py` en de bijbehorende `related_to`-relaties in `concepts.json` (bv. `geluid → muziek`, `vloeistof → water`) bestonden al, maar waren via geen enkele natuurlijke zin bereikbaar — `detect_definition()` had wel blokken voor synoniemen/antoniemen/used_for/causes/properties, maar nooit een voor related_to. Toegevoegd: een nieuw detectieblok (`"waarop lijkt "`, `"waar lijkt "`, `"wat lijkt op "`) volgens exact hetzelfde patroon als de bestaande vier blokken, incl. lidwoord-stripping. Getest en werkend: "waarop lijkt geluid" → "geluid lijkt op: muziek", plus het "geen resultaat"-pad ("waarop lijkt een hond" → "Ik weet niet waarop hond lijkt.").

**microlearning.py — VOLLEDIG GEBOUWD EN GETEST (17-18 juli 2026).** Layer 6 Fase 6 (Adaptive Learning), zie de uitgebreide sectie onder 7-Laags Memory Architectuur in `nova_state.md`. Was lang leeg, nu compleet: signaal-classificatiemodel, tempo-gebaseerde trait-verschuivingen binnen harde grenzen, live-koppeling, automatische hertraining met ijkpunt-veiligheidsrem.

**Layer 2 opruimwerk** — opgelost (18 juli 2026). `MIN_OBSERVATIES_VOOR_ANOMALIE` teruggezet van tijdelijke testwaarde 3 naar 10. Opslag omgebouwd van tellergebaseerd (elke 2 observaties, `total % 2 == 0`) naar écht tijdgebaseerd via een eigen achtergrondtimer (`SAVE_INTERVAL_SECONDS = 300`, enkel bij wijzigingen dankzij `self._dirty`-vlag) — zelfde principe als `memory.py`'s write-buffer. Nieuwe `shutdown()`-methode toegevoegd zodat de laatste, nog niet weggeschreven wijzigingen bij `/reboot` en `exit` niet verloren gaan (automatisch opgepikt door `reboot_manager.py`, expliciet toegevoegd aan `exit` in `main.py`). Bijkomende project-brede audit (18 juli 2026) bevestigde dat `memory.py` (atexit-hook), `word_associations_learner.py` en `context_manager.py` (allebei meteen opslaan, geen buffer) geen vergelijkbaar risico liepen — `pattern_matcher.py` was de enige module met dit gat. Zie Layer 2-sectie onder 7-Laags Memory Architectuur in `nova_state.md` voor volledige details.

**Layer 5 Fase 1-5 (Context Manager, Activity/Focus/Presence Detection, Interruption Logic) — VOLLEDIG AFGEROND (13-16 juli 2026).** Fase 1: tijd + Layer 2-koppeling. Fase 2: activiteit-detectie via venstertitel (`activity_detector.py`, `pygetwindow`). Fase 3: focus-detectie via input-timing (`focus_detector.py`, `GetLastInputInfo`). Fase 4: aanwezigheidsdetectie via webcam (`presence_detector.py`, MediaPipe Tasks API — eerste bounded ML-tool in Layer 5). Fase 5: `_bepaal_interrupt()` omgebouwd van "eerste match wint"-volgorde naar een gewogen SCORE-systeem (blijft 100% symbolisch, vaste gewichten); "niemand aanwezig" blijft een harde stopregel buiten de score; nieuw `response_style`-veld (`"kort"/"normaal"/"uitgebreid"`) berekend, sinds 17 juli 2026 ook geïntegreerd. Zie sectie "Layer 5 — Context Manager" onder 7-Laags Memory Architectuur in `nova_state.md` voor volledige details.

**Layer 6-integratie van `response_style` (Layer 5 Fase 5's veld) — VOLLEDIG AFGEROND (17 juli 2026).** `context_manager.py` berekende en publiceerde al `response_style` (`"kort"/"normaal"/"uitgebreid"`) via `context:updated`, maar niemand raadpleegde het. Nu volledig aangesloten: `response_pipeline.py` haalt het op bij elk antwoord, `chat_response_engine.py` geeft het door, en `expression_injector.py` gebruikt het om emoji's/gestures/uitroeptekens volledig te onderdrukken bij "kort". Zie de sectie "Layer 6 — Personality/Identity écht aangesloten op de tone-pipeline" onder 7-Laags Memory Architectuur in `nova_state.md` voor de volledige uitleg incl. de 6 gewijzigde bestanden.

**emotion_engine.py decay/recovery-mechanisme** — opgelost (11 juli 2026), zie bug #4/#11 hierboven. `overstimulation.level` heeft nu zowel tijd- als interactie-gebaseerde decay.

**Layer 6 — identity-blueprint aangesloten op de tone-pipeline — VOLLEDIG AFGEROND (17 juli 2026).** Ontdekt tijdens bug #4/#11-onderzoek (11 juli 2026), opgelost 17 juli 2026:
- **`personality_engine.py`**: `generate_response_style()` (`pace`/`tone`/`interrupts`/`dramatic`) wordt nu ECHT gebruikt door `expression_injector.py` om de uiteindelijke tekst te beïnvloeden, niet enkel doorgegeven in event-data. Zie Layer 6-sectie in `nova_state.md` voor de volledige keten.
- **`identity_state.json → memory.py`**: `PersonalityEngine.update_state()` publiceert nu `identity_state:updated`, automatisch opgevangen door `memory.py`'s bestaande wildcard-subscribe — Nova's stemmingsgeschiedenis is nu terug te vinden in `interactions.jsonl`/`interactions.db`.
- **`behavior_modifiers.py`** (`BehaviorModifiers`) blijft nog steeds ongebruikt — dit werd bewust NIET meegenomen in de 17 juli-sessie (zie Layer 6-sectie voor scope). Zijn methodes (`apply_energy_modulation()`, `apply_impulsivity()`, `apply_dramatic_flair()`) worden nog steeds nergens aangeroepen. **Dit is een klein, nog open werkpunt — blijft daarom ook vermeld in `nova_state.md`.**
- **`identity.json`'s rijkere velden** (`regulation_profile`, `sensorimotor_profile`, `embodied_cognition`, `interaction_nuance`, enz.) blijven ongebruikt buiten schema-validatie — dat raakt eerder Layer 7 (Emergent Behaviors) en de toekomstige avatar/embodiment-fase, niet Layer 6 zelf.

Zie de volledige sectie "Layer 6 — Personality/Identity écht aangesloten op de tone-pipeline" onder 7-Laags Memory Architectuur in `nova_state.md` voor alle details, gewijzigde bestanden en testresultaten.

*Aanvulling (12 juli 2026):* los van dit werkpunt bestaat ook `identity/self_query.py`, dat `identity.json` (via `loader.py`) en de live `emotion_state.json` uitleest om vragen als "wie ben je"/"wat vind je leuk"/"hoe voel je je" te beantwoorden — een apart, direct pad van vaste blueprint-velden naar spreektekst, zonder over de personality/behavior-laag te lopen. Zie sectie "Zelfkennis-laag" in `nova_state.md` voor details.

**Fundamentele voorwaarde voor proactief gedrag: `main.py`'s blocking `input()`-lus — opgelost (12 juli 2026).** Achtergrondthread + directe EventBus-subscriber op `chat_response` gebouwd en getest met een eerste echte proactieve feature (`session_watcher.py`, pauze-melding na inactiviteitsdrempel). Volledige uitleg van het patroon in de sectie "Proactief spreken — het achtergrondthread-patroon" in `nova_state.md`.

---

## 📌 Nog open (blijven in `nova_state.md`, hier enkel ter referentie)

- Bug #8, #9, #10
- Werkpunt: User preferences-module, memory.py Fase 5, Intent classifier, Activity Awareness, Activity-Aware Interaction, `behavior_modifiers.py`-koppeling